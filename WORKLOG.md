# 작업 기록

## 2026-03-30 작업 내용

### 1. Vite/React 소스 커밋 (d25bcba)
- `src/`, `package.json`, `vite.config.js`, `tailwind.config.js` 등 미커밋 상태였던 Vite/React 소스 파일을 git에 추가
- 이 파일들은 실제 서비스에는 사용되지 않음 (참고용)

### 2. 퀴즈 시스템 대폭 개선 (3063ee4)
실제 서비스 파일(`app.js`, `data.js`, `style.css`)에 반영:

#### 퀴즈 문제 확장
- 각 레벨 5문제 -> **20문제**로 확장 (총 500문제)
- 문제 유형: cn_to_kr (중->한), kr_to_cn (한->중), fill_blank (빈칸)

#### 랜덤 출제
- 퀴즈 시작 시 20문제 중 **10문제를 랜덤 선택**
- 매번 다른 문제가 출제됨

#### 통과 기준 변경
- 기존: 5문제 중 4문제 (80%)
- 변경: **10문제 중 8문제 (80%)**

#### UI 변경 - PASS 리본 배지
- 통과한 레벨의 아이콘 위에 **대각선 노란-주황 PASS 리본** 표시
- 기존의 별점(⭐ X/5) 표시는 제거

### 주의사항 (중요!)
- 이 프로젝트는 **순수 정적 페이지**로 서비스됨
- `index.html` -> `app.js` + `data.js` + `style.css`
- `src/` 폴더의 Vite/React 코드는 서비스에 사용되지 않음
- 기능 변경 시 반드시 `app.js`, `data.js`, `style.css`를 직접 수정해야 함
